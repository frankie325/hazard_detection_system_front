# 2026-04-05

## 大屏色系改造：浅蓝科技风

- 将 bigScreen/index.vue 的色系从深黑色调改为浅蓝科技风格
- 核心变量体系：深蓝背景渐变 `#0b1a2e → #0f2645`，半透明卡片 `rgba(12,30,56,0.75)`
- 主色调：`#00c8ff`（科技青）、`#4d9eff`（蓝）、`#7cb9ff`（浅蓝）
- 新增科技感网格纹理（伪元素 `::before`）、标题渐变文字、header 底部发光线
- 面板使用 `backdrop-filter: blur(8px)` 毛玻璃效果
- ECharts 图表配色同步更新（柱状图 + 环形图）
- SVG 摄像头标记颜色更新：正常 `#00c8ff`、告警 `#ff5a5a`
- CardWrapper 组件的渐变边框和背景色同步调为蓝色系
- 告警标签语义色优化：紧急红、高级橙、中级黄、低级青，背景透明度降低更和谐

## 大屏 Header 改造：科技扫描线 + 装饰翼

- header 高度从 56px 调为 64px，padding 增大
- 新增 `header-center` 居中布局，包含标题 + 左右装饰翼
- **装饰翼**：左右各一条主线 + 辅线 + 菱形端点，带呼吸发光动画
- **扫描线**：一条半透明青色光线从左到右扫过标题区域（4s 循环）
- **标题下划线**：渐变发光线 + blur 模拟脉冲呼吸效果
- **背景粒子**：6 个不同大小/位置/速度的浮动光点
- 左侧增加日期显示（年-月-日 + 星期），时间字号加大为 18px
- 用户已引入 vue-bits 的 GradientText 组件处理标题字体特效

## Header 布局调整

- 左侧时间移到右侧，移除"数据同步中"显示
- 左侧新增返回按钮（router.back），带 hover 青色高亮和 active 缩放效果
- 右侧显示日期 + 时间

# 2026-04-06

## 登录页样式改造：参考 Figma 设计稿

- **Figma 文件**：高速道路危害识别系统 (file: pJllwEnMl62N4XmhNfbwjq, node: 9-2)
- **Figma Token** 已配置到 .env 中 `FIGMA_ACCESS_TOKEN`
- **LoginLeftView 组件**完全重写：
  - 移除原有的几何装饰元素（圆圈、方块、泡泡等）
  - 实现地图纹理背景（网格 + 道路线条模拟 + 标注点）
  - 双层渐变遮罩：左→右白色0.6渐隐 + 底部→顶部白色0.2渐隐
  - 两个192px毛玻璃装饰色块（`#0060AF` + `#00677F`，blur 80，radius 12）
  - 布局：顶部 Logo+系统名（Inter 20px 700 #0060AF）、中间大标题（Space Grotesk 60px 700）、底部系统信息
  - 暗色模式完整适配
- **style.css** 重写：
  - Figma 设计稿 CSS 变量体系（`--login-primary: #0060AF` 等）
  - 标题从 `text-4xl font-semibold` 改为 `text-[30px] font-bold`
  - 输入框圆角改为 2px，高度 40px，focus 边框色 #0060AF
  - 登录按钮高度 52px，圆角 2px，背景 #0060AF，双层蓝色阴影
  - 链接文字 12px 700 主题色
- **index.vue** 更新：
  - SplashCursor 背景色从 `{ r: 0.5, g: 0, b: 0 }` 改为动态计算（亮色 `#FDFCFF`，暗色 `#070707`）
  - 添加 `.login-page` 背景色 `#fdfcff`
