# 2026-04-07

## 设备监控面板改造：上下布局 + 视频合并

- **device-info-panel.vue** 完全重写：
  - 改为上下布局：视频预览区在上（340px 高），设备信息与控制在下
  - 合并了原 `video-preview-panel.vue` 的视频预览功能，移除了独立的"视频预览"Tab
  - 视频区：深色顶栏（设备名 + 播放状态标签 + 操作按钮），视频 340px 高，支持打开/关闭实时画面
  - 视频直接用原始 `videoUrl` 通过 `<video>` 标签播放（autoplay/muted/controls）
  - 检测预览弹窗改为 90% 宽度，视频充满 75vh 高度，左上角标注"MJPEG 检测流"
  - `el-dialog__body` padding 设为 0，让视频完全撑满
  - 摄像头类型显示视频区 + 检测预览按钮；传感器类型只显示设备信息
  - 设备信息改为 3 列布局（`el-descriptions :column="3" size="small"`）
- **index.vue** 更新：移除 VideoPreviewPanel 导入和独立的视频预览 Tab
- 0 lint 错误

## AlarmLevelDot 组件迁移 + 脉冲光晕特效改造

- 组件从 `src/components/core/base/AlarmLevelDot/` 移动到 `src/components/AlarmLevelDot/`
- **新特效方案**（第4种：脉冲圆点 + 呼吸光晕）：
  - 🔴 紧急：1s 快速脉冲 + 大光晕(blur 6px) + 圆点轻微抖动
  - 🟠 高级：2s 中速脉冲 + 中光晕
  - 🟡 中级：3s 慢速脉冲，无光晕
  - 🔵 低级：静态圆点，无动画
- 新增 `hideLabel` prop 支持仅显示圆点
- 新增 3 个 CSS 动画：`pulse-expand`(扩散)、`glow-breathe`(呼吸)、`emergency-shake`(抖动)
- 颜色方案调整：紧急 #ef4444、高级 #f97316、中级 #eab308、低级 #3b82f6
- 更新了 8 个引用文件的 import 路径：warning/rule、warning/message(4个)、emergency/event-center(3个)
- `components.d.ts` 自动更新了路径声明
